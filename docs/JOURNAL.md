# Optimization journal — qr_v2

> Milestone filenames were normalized on 2026-09-11 to `NN_<name>_official<score>us.py`; three of
> them had been carrying *Modal lab* numbers rather than official ones. Per-file provenance is in
> [`../milestones/README.md`](../milestones/README.md).

Geomean (µs). "Modal" = my `gpu_bench` on Modal B200 (optimistic on tf32/fp16, accurate on geqrf/FP32/panel). "Official" = real gpumode leaderboard.

| step | file | geomean | what changed |
|---|---|---|---|
| baseline (eager) | milestones/01_eager_official123203us.py | **123203 official** | blocked compact-WY, all eager (≈250+ launches/call, dispatch-bound) |
| triton panel | milestones/02_triton_panel_official43576us.py | **43576 official** | fused Triton sub-panel kernel + bmm trailing (still has the ~60-launch dlarft loop) |
| cand_D | experiments/cand_D_hybrid.py | 18448 Modal | **3×TF32** emulated-fp32 (hi/lo split + 3 matmuls) + shape-route geqrf for tiny/small-batch + **T via one triangular solve** (kills dlarft loop) |
| cand_H | experiments/cand_H_blktune.py | 16568 Modal | block tune: n=176→64 (5315→1083 µs); n=352/512/1024→32 (block 16 too small) |
| cand_I | experiments/cand_I_baddbmm.py | 14712 Modal | 3×TF32 accumulate via `baddbmm` (kills the 2 big add-tensor passes); helped n=176/352/1024, n=512 barely (→ n=512 is GEMM-shape-bound) |
| cand_J | experiments/cand_J_2level.py | 13940 Modal | **two-level blocking** NB=128/ib=32 (fat K=128 trailing GEMM); n=512 25.4k→19.7k |
| cand_M | experiments/cand_M_1x1024.py | 13222 Modal | **1×TF32 for n≥1024** (looser 20·n·eps tol; passes mixed/nearrank); n=1024 37k→29k |
| cand_O | experiments/cand_O_NB256.py | ~12000 Modal | two-level **NB=256** (fatter K=256); n=512 19.8k→18.7k. 22/22 pass. |
| tf32x3 | milestones/03_tf32x3_2level_official14760us.py | **14760 official** | fused `tl.dot(input_precision="tf32x3")` batched-GEMM kernel (no hi/lo HBM split); Modal said ~10.7k but official 14760 ⇒ **Modal calibration discovered** |
| **panel-warps** | milestones/04_panel_warps_official8580us.py | **8580 official** | launch `_panel_kernel` with `num_warps=4/8/16` by tile size. The per-CTA tile work was starved at 4 warps. **n=1024 54ms→10.9ms = 5× officially** (Modal only showed 2×). 1.72× official jump. |
| **fused-panel** | submission.py (m5 + ib=64/NB=128 for n=512) | n=512 ~16.5k→~14.4k Modal | widen ib 32→64 (SRAM-max fused panel) + NB 256→128 for n=512; n=1024 unchanged. ~7% apples-to-apples (noise-swamped). 22/22. Full mega-kernel KILLED first by `microbench_trailing.py` (single-CTA trailing 1.4-3.8× slower than batched). |
| route n=2048 | (folded into m5) | **~8100 est** (Modal 8117) | route n=2048 (b=8) to the custom one-CTA panel+warps instead of geqrf: 76.8ms→35ms (2.2×). Official gain partial (n=2048 trailing is 1×TF32, Modal-optimistic). NOT yet officially submitted. |
| **gram-route** | (engine step 1) | **6907 Modal** (from 7793 same-run baseline = 1.13×) | ENGINE ATTACK step 1. Per-phase profiler (`experiments/profile_phases.py`) overturned "trailing GEMM is THE gap": at n=512 it is only 26% (panel 41%, gram 15%, Tsolve 18%); at n=1024 only 21% (narrow-updates 49%, gram 29%, Tsolve 21%). First lever: the T-factor Gram VᵀV (`_gram`) was unconditionally tf32x3 hi/lo-split; route it like the trailing — fused tf32x3 for n≤512, **1×TF32 for n≥1024** (V is well-conditioned unit reflectors). n=512 14.5→13.4k, n=1024 10.8→8.8k, n=2048 34.6→27.3k; 22/22; mixed margins unchanged at the 2.0× safe floor. |
| **panel nw=8** | submission.py (current) | **5678 Modal** (from 5756 = 1.4%) | ENGINE ATTACK step 6 (panel). `microbench_panel.py` diagnosed the panel as **LATENCY-bound** (per-col time ~flat as ib grows; warps past 8 don't help → it's the sequential reflector-reduction chain, NOT the rank-1 FLOPs → tl.dot micro-blocking won't help). First-sub-panel best `num_warps=8` for BN=512/1024/2048 (nw=16/32 were 8-13% slower, nw=4 6× slower). Old schedule over-provisioned (16 for n=1024, 32 for n=2048) → changed to `nw = 4 if BN<=128 else 8`. n=1024 6.9→6.6k, n=2048 17.5→16.0k; n=512 unchanged (BN=512 was already 8); 22/22, margins held. n=2048 panel also occupancy-starved (8 CTAs → 13.6µs/mat vs 5.6 saturated; needs multi-CTA = grid-sync = research-grade). |
| **glue-fusion** | (engine step 5) | **5756 Modal** (from 6771 = 1.18×) | ENGINE ATTACK step 5 (step 4 = raw-PTX trailing, BUILT but DEAD — see DEAD_ENDS). Detailed `torch.profiler` op-map (`experiments/profile_detailed.py`) found ~20% is elementwise GLUE around the math (not the math). Removed it in `_apply_block`: (a) dropped the redundant `.clone()` after `torch.tril` (was a full m×b Memcpy DtoD per apply), (b) fused `C.sub_(_mm(V,Y))` -> `_mm_sub`: tf32x3 subtract-epilogue kernel `_bmm_x3_sub_kernel` for n≤512 / `baddbmm_` for n≥1024 (kills the separate matmul output + sub_ kernel), (c) diagonal-view fill/copy instead of arange+fancy-index. Per-case: n=176 627→449, n=352 1785→1363, n=1024 8.5→6.8k, **n=2048 23.9→17.5k (1.36×, most applies)**; n=512 untouched-precision (mixed 2.0×); 22/22. BUG caught: the in-place sub kernel needs `restore_value=['C']` in autotune (else autotune re-runs corrupt C — failed n=176/352 stress, would DQ). **Cumulative engine session: 7793→5756 Modal = 1.35×.** CALIBRATION: Modal now ≈ OFFICIAL (V4 Modal 7793 ≈ official 7788), so this ≈ official ~5800. NOT yet officially submitted. |
| **adaptive-ib** | (engine step 3) | **6771 Modal** (from 6907 = 1.02×) | ENGINE ATTACK step 3 (step 2 = Tsolve-via-inverse, REJECTED — see DEAD_ENDS). Grow sub-panel width `ib` as remaining height m shrinks (`_max_ib`), using the SRAM headroom that opens marching down the matrix — fewer + fatter (K=64/128) narrow updates instead of many K=32 ones (n=2048 narrow-apply count 120→78). `_panel_kernel` byte-identical (panel stays fp32). n=2048 27.3→23.9k (**1.14×**), n=1024 8.8→8.5k; n=512 untouched (precision floor safe); 22/22, mixed margins 2.0×. Designed by a 4-approach workflow (naive "raise ib" is broken — rounds to 256KB>SRAM; recursive doesn't cut apply count). Cumulative engine session: 7793→6771 Modal (1.15×). NOT yet officially submitted. |
| **V5 confirmed; fp8-fp4-attack session (2026-06-26)** | submission.py = **V5 tf32x3** (unchanged; net revert) | **official 5915 (V5) = best @2026-06-26**; V6 fp16x3 = 5997 WASH | V5 (tf32x3 = the engine-attack result) officially confirmed **5915µs** (id 834868). Branch `fp8-fp4-attack` explored sub-tf32, NET-REVERTED to V5: **(1) FP16x3 trailing** — Modal lab said +3.6% but **official submissionV6 = 5997 ≈ V5 5915 = WASH** (fp16 uses fp32-accumulate → runs at the tf32 rate; the 2× fp16 peak is fp16-ACC only; Modal gain was a Triton codegen artifact) → reverted. **(2) fp8/fp4 = SPEED-DEAD, rigorously** (PTX confirms `tcgen05.mma` dedicated cores ARE used; `torch._scaled_mm` = 100% fp8 peak @4096³ but **0% @our 512×128×384 shape** → the K-poor batched shape is the wall, not the API; mxfp8 `dot_scaled` 1.16-1.29× slower than plain fp8; fp4 = fp8 throughput on dense B200). Accuracy-viable though (fp8 6-dot Ozaki = mixed@640 m10.8). **(3)** the mixed@640 "1.9× floor" = a **tf32 triangular-SOLVE artifact** (allow_tf32=True → cuBLAS trsm in tf32); solve→fp32 → ~800× margin (+~1%, a robustness option, not speed). **(4)** GEMM feeding curve (`microbench_feed_gemm.py`): K-richness feeds tf32/fp16 (47%→93% K128→K512) but NOT fp8 (caps 30%). **Net official speed gain ≈ 0.** Red-team verdict: the real wall is the **41% sequential-reflector panel** (Householder's math); the one un-dead lever = a Gluon warp-specialized **panel/trailing overlap** (research-grade — NOTE: the NEXT row measured this DEAD). Full record: `archive/docs/FP8_SESSION_PROGRESS.md`. |
| **research session (2026-06-26): overlap + ALL levers measured-DEAD** | submission.py = **V5 tf32x3** (UNCHANGED) | **official 5915 = best @2026-06-26 (net 0 speed)** | The "panel/trailing overlap" lever above + every other candidate measured-DEAD on B200: (1) Gluon warp-spec OVERLAP design A — `gl.warp_specialize` exists + async tcgen05 overlaps (eff 0.6–1.0), but the panel can't co-reside (register panel blows the 16-warp/64K budget; smem-streaming panel 2.56–6.74× too slow = register-gate ⟂ speed-gate, mutually exclusive). (2) recursive/K-rich blocking 0.976×, cuBLAS-trailing 0.866×. (3) CholeskyQR floor-breaker `_LinAlgError` @n=2048/4096 + conditioning-routing = v2-forbidden reward-hack. (4) multi-CTA cooperative panel — hand-rolled atomic grid barrier WORKS (grep-clean, ~2.6µs, overturns "grid-sync=banned") but panel per-column work (~0.66µs) < barrier → 7–8× slower. (5) TLX infeasible on Modal. The leaders' edge = a multi-week warp-spec PERSISTENT tcgen05 engine (TMA + multi-stage pipeline + 2-SM tiles) + a fast-AND-co-residable panel we haven't built (gau.nernst 1558µs proves the pure-Gluon route exists). Remaining crux = MAGMA register-file panel (½-day gate) → design B. Autonomous plan: `archive/docs/HANDOVER_NEXT_SESSION.md` + `archive/docs/NEXT_SESSION_PROMPT.md`. |
| **glue-fusion V8/V9 (2026-06-27)** | `milestones/09_V9_gluefuse2_official5791us.py` | **official V9 = 5791** (V8 = 5898) | V9 = V5 + structural glue fusion (`_build_M`/`_build_Mt`/`_build_V`), all bit-identical; Modal +7.4% → official +2.1%. Most branches' `submission.py` = V9. |
| **V10 fused-Triton (n≤512)** | `milestones/10_V10_fused_official5343us.py` (recovered from branch `persistent-engine` 2026-09-11) | **official 5343** (id 838292; Modal 4913) | one-shot fused Triton kernel for n≤512 (1 CTA/matrix, in-kernel blocked-Householder + LARFT + apply); kills V9's ~32 launches + HBM round-trips; −7.7% vs V9. |
| **CURRENT BEST (2026-06-28)** | `milestones/11_V10sub5_fused32_official4247us_BEST.py` (codex `xinbranch`) | **official 4247 (id 840028) = BEST** | V10 + route CUDA n≤64/B>16 to the fused Triton QR kernel (IB=32, nw=2) instead of geqrf — fixes the b20 n32 dense cuSOLVER cost (318→30µs). Modal 4918→4035 (1.22×); official 4247 ≈ Modal−4%. The cute-DSL engine is NOT involved (still research-stage, ~20× off). |

## Key technical facts established (with evidence)
- **~1000× FP32 tolerance margin:** plain FP32 Householder gives scaled_factor_residual ≈ 0.016 vs gate 20.
- **Mantissa wall at batch=640:** 1×TF32 (10-bit mantissa) fails n=512 mixed@640 (worst-of-640 → scaled 21 > 20). Passes n≥1024 (looser tol) and n=512 dense/rankdef/clustered. A batch-16 probe MISSES this — always gate mixed at the real batch (640).
- **tf32x3 (3-pass tf32, FULL fp32 exponent range) passes all 22** incl clustered's 7-order intra-matrix range (fp32 exponent + fp32 accum + residual-relative-to-‖A‖).
- **The hi/lo split (for 3×TF32 in pytorch) was the cost, not the matmul count** — 1×TF32 n=512=10.7k vs split-3×TF32=19k; going 2-term→3-term was free. The fused tf32x3 Triton kernel removes the split HBM traffic.
- **Panel is reduction/SRAM-bandwidth bound** (pure `tl.sum` + elementwise, NO tl.dot). So the lever is PARALLELISM (warps), not precision. The panel-warps win confirms this.
- **geqrf floor:** n=2048 77ms, n=4096 52ms via cuSOLVER (FP32, parallelizes one matrix across SMs). Custom one-CTA-per-matrix beats it ONLY with enough CTAs (=batch): n=2048 b=8 wins 2.2×; n=4096 b=2 loses 3.9× (2 CTAs).
- **Modal calibration (REFINED 2026-06-25, then 06-26):** the old "~1.5-2× fast" was from the EARLY `submission_v2` before the full-warmup pass — STALE. CURRENT: with `gpu_bench`'s warmup, Modal is **~4% OPTIMISTIC** on absolute speed (V5 Modal 5678 vs official 5915; V4 7793 vs 7788) — trust it for correctness + RELATIVE ranking, ×~1.04 for absolute. ⚠️ **06-26 lesson:** RELATIVE gains from codegen-sensitive micro-opts can FAIL to transfer (fp16x3 = Modal +3.6% but official WASH) → gate any sub-5% "win" on a real submission. See memory `qr-v2-testing-setup`.
