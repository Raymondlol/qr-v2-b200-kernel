# Next steps — the evidence-based path forward (updated after the full investigation)

> **▶ `docs/HANDOVER_NEXT_SESSION.md` is the FINAL STATE after the 2026-06-26 research session + `docs/NEXT_SESSION_PROMPT.md` is the autonomous plan.** The Gluon warp-spec OVERLAP staged plan that used to live here is now measured-DEAD (design A blows the register budget; smem panel too slow). Also DEAD this session: CholeskyQR (LinAlgError + conditioning-routing-forbidden), multi-CTA cooperative panel (barrier works but per-column granularity too fine, 7-8× slower), recursive-blocking (0.976×), cuBLAS-trailing (0.866×). The one remaining crux = a fast-AND-co-residable panel (register-file panel ½-day gate → design B). This file (below) is the broader ranked-levers reference (some entries now superseded by DEAD_ENDS).

## Immediate
**CURRENT BEST = V5 tf32x3 (official 5915µs); `submission.py` is reverted to it.** FP16x3 was tried (submissionV6) — Modal said +3.6% but **OFFICIAL = 5997 ≈ V5 5915 = WASH** (fp16-fp32acc = tf32 rate; the Modal gain was a codegen artifact that did NOT transfer). So there is NO pending speed win to submit. The deployable, confirmed-valuable option from this session is the **solve→fp32 robustness fix** (apply on V5 tf32x3): mixed@640 margin ~1.9×→~800× at ~+1% — worth a submission ONLY if you want reseed-DQ insurance over ~1% speed. `grep -niE "stream|graph" submission.py` → empty (confirmed). LESSON (the Modal-vs-official calibration note in `docs/METHODOLOGY.md`): gate any sub-5% Modal/codegen "win" on a real submission before trusting it. Run the lab harness for any new candidate (`docs/METHODOLOGY.md`).

## Where we are
Last confirmed **5915 µs official** (V5, tf32x3); `submission.py` = **V5 tf32x3** (FP16x3 was REVERTED — official wash; do NOT believe the stale "V5+FP16x3=5947" that used to be here), 22/22; leader **1292 µs**. The CHEAP deployable levers are now exhausted (gram/adaptive-ib/glue/panel-nw8/blocking/fp16x3). **The trailing-GEMM precision question is RESOLVED:** FP16x3 is the deployable win (tf32x3-class accuracy at 2× the tensor-core throughput); fp8/fp4 are MEASURED speed-dead (dedicated 5th-gen tcgen05 cores ARE used, but the K-poor batched trailing SHAPE caps them at ~30% of fp8 peak even K-rich, and fp8 needs 2× the Ozaki dots → ~2× slower than tf32x3); and the old mixed@640 "1.9× tf32x3 floor" was a tf32-**solve** artifact (forcing solve→fp32 gives ~800× margin), NOT a trailing wall. So the trailing has ~800× precision headroom at n≤512 and the GEMM precision is no longer where the gap lives. Profiler verdict stands: the bottleneck is the **PANEL (~40-50%, latency-bound by the sequential reflector reduction chain)** — NOT the trailing GEMM (21-26%). The remaining gap is research-grade: a fundamentally different panel kernel (my current one is tapped in Triton; a two-level peer hits 3352 so ~1.7× headroom likely exists in-family) OR the deployable-but-bounded Gluon tcgen05 engine (branch `gluon-tcgen05`).

## What's left, ranked by EV — UPDATED with the 2026-06-25 engine baseline

**Measured trailing-GEMM efficiency (`experiments/microbench_floor_and_engine.py`, B200):** on the REAL trailing shapes even *cuBLAS 1×TF32* reaches only **21–34 % of tf32 peak on the FAT updates** (n=512 fat 236 TFLOP/s/21 %; n=1024 fat 377/34 %) and a dismal **6–7 % on the THIN K=32 within-panel updates** (75 and 70 TFLOP/s). Our fused tf32x3 Triton kernel runs at **~10 % of its ÷3 ceiling and is ~6× slower than cuBLAS 1×TF32** on the fat shapes (n=512 fat: cuBLAS 136 µs vs ours 841 µs — ~2× slower even after normalizing for tf32x3's 3× work). So the old "~1.5 % of peak" figure was too pessimistic, but the headroom is real and it is **structured** — attack it in this order:

### 1a. Recursive / bigger-K blocking — kill the 6–7 % thin K=32 updates (EASIEST, structural; ~6 % BOUNDED)
The two-level scheme does many narrow K=32 within-panel updates that run at ~6 % even on cuBLAS. Recursive blocking (K=n/2 via recursive QR; widen K) converts these into the fat shapes that already hit 21–34 % — the feeding curve (`experiments/microbench_feed_gemm.py`) shows tf32/fp16 efficiency ~doubles K128→K512 (47%→93%), so this lifts fp16x3/tf32x3 trailing **~1.5×**. No new kernel — a blocking change, highest EV-per-effort first step. BUT three caveats: (1) it is **Amdahl-bounded to ~6 % geomean** because the trailing is only 21-26 % of runtime; (2) it does **NOT unlock fp8** (caps ~30 % of fp8 peak even at K=1024 — M/N too small to fill fp8's wider tiles); (3) bigger K = a **wider sequential panel cost**, partially eating the trailing win. Still the best deployable trailing lever; the floor is the panel, not GEMM feeding.

**CholeskyQR algorithm pivot (reiterated — the ONLY way to move panel work into a fed GEMM):** the Gram A^T A has K=m=512 = the *richest* GEMM in the problem, and computing it relocates the latency-bound sequential reflector chain into one fat, fully-fed tensor-core GEMM. This is the only lever that attacks the real (panel) bottleneck rather than the 21-26 % trailing slice. BUT it re-imports the **Cholesky + reconstruction floor** (chol itself is floor-bound on the stress cases) and the fp32-Gram accuracy need → bounded, mid-board per `HOW_LEADERS_ARE_FAST.md`. Naive CholeskyQR is already DEAD (cuSOLVER-slow + floor); a *custom blocked tensor-core* Cholesky + unpivoted-reconstruction is the only version worth building, and only if one is being built anyway (cross-refs §2).

### 1b. Best-achievable trailing impl — is our fused tf32x3 kernel even the right call?
Our fused kernel is ~2× below cuBLAS on the fat shapes (after the tf32x3 factor). Re-test: 3×cuBLAS hi/lo split tf32x3 vs the fused Triton kernel vs a better-tuned/larger-tile Triton tf32x3, on the real shapes, same run. The journal's "fused beat the split" was an end-to-end Modal claim; the raw-GEMM gap (6×) says re-measure. Cheap, may give a quick 1.5–2× before any hard kernel work.

### 1c. Warp-specialized / raw-PTX trailing engine — ATTEMPTED, BUILT, CONFIRMED NOT WORTH IT (2026-06-25)
Built a correct cp.async double-buffered tf32x3 `mma.sync.m16n8k8` GEMM via load_inline (`experiments/cuda_gate{1,2,3}.py`, `modal_cuda.py`): ~1.2× over Triton's tf32x3 (51 TF/s n=1024). DEAD anyway — see DEAD_ENDS for the full writeup. Three compounding reasons: (1) trailing GEMM is only 26%/21% of n=512/n=1024 and n≥1024 already uses faster 1×TF32, so it only helps n=512 → **~1.5–2% geomean**; (2) **TLX absent**, persistent/TMA Triton variants worse; (3) **load_inline needs nvcc on the competition machine** (torch-only env doesn't have it) → likely undeployable. **The gap is NOT a faster trailing GEMM.** The real trailing lever is the DEPLOYABLE **bigger-K shape** (1a/1b): K-poor shapes cap even cuBLAS at 20–34%; bigger NB (now that adaptive-ib absorbs the narrow-update cost) lifts the existing kernels' efficiency — UNTESTED, cheap, the recommended next trailing experiment. The biggest single n=512 phase remains the **panel (41%, latency-bound by the sequential reflector dependency)** — needs a different panel algorithm, not a faster GEMM.

### 2. n=4096 floor-breaker — PARKED (bounded ~3 %, needs 2–3 custom kernels)
n=4096 b=2 = 52 ms via geqrf. Floor-probe showed a *blocked* Cholesky already beats cuSOLVER 2.86× (8.9 ms) — so a custom CholeskyQR floor-breaker is not dead — but the full path (CQR3-safe blocked chol + custom unpivoted-GETRFNP reconstruction) is ~35 ms = ~1.5× on ONE of twelve cases ≈ ~3 % geomean, for 2–3 hand-written kernels. Lower EV than the engine; revisit only if a custom blocked tensor-core Cholesky gets built anyway. (cooperative multi-CTA Householder panel is the alternative floor-breaker — same hardness, also parked.)

### 3. Risky ~3 %: fused 2-term-rounded trailing (`experiments/cand_fused2br.py`)
Passes all 12 on benchmark seeds, ~5 % faster on n=512, but only **1.4× margin** on mixed@640 → reseed-DQ risk. Only if you accept the risk (can resubmit if it fails).

### 4. Register-file panel (MAGMA-style) + recursive blocking — incremental, uncertain.

## Dead — do NOT spend time here (proof in DEAD_ENDS.md)
Mega-kernel / single-CTA trailing; naive CholeskyQR; lowprec-panel; TSQR-output; expanded GEMM autotune; stream/graph anything (illegal). **fp8/nvfp4/mxfp = now MEASURED dead-on-speed** (not just estimated): dedicated 5th-gen tcgen05 cores ARE emitted and accessible (torch._scaled_mm hits 100 % fp8 peak at 4096³), but our K-poor per-matrix 512×128×384 trailing caps fp8 at ~2 TFLOP/s = ~0 % of peak; a fully-fused autotuned fp8-Ozaki-6dot kernel is only 14 % faster than fused tf32x3 (and 3.7× slower realistically once per-tensor scales are paid), mxfp8 `dot_scaled` is 1.16-1.29× SLOWER than plain fp8, fp4 = fp8 throughput on dense B200 + needs more dots → strictly worse. **1×TF32 trailing = reseed-DQ** (mixed@640 worst-of-640 sfr 19.7, margin 1.02, gate 20). **Benchmark-structure early-stop / per-matrix conditioning-routing = rule-violating reward-hack — do NOT use** (the Codex `verify-qr-v2-new-levers` branch in the MAIN worktree: `_structured_stop` inspects matrix[0] and hardcodes (n,B) to early-stop the whole batch; its logged ~5165 was the hack skipping work, not a speedup — the v2 rewrite exists specifically to forbid this).

## Validation discipline (Modal lies on tf32 absolute speed)
`modal run modal_app.py --submission <f> --stress` (22/22) before any submit; **always gate mixed at batch=640** (the STRESS probe is batch-16 and misses the 1×TF32 failure — the benchmark cases gate it). Compare candidates in the SAME modal run (count=1 noise). Confirm absolute speed via real submission. Panel/FP32 changes: Modal is reliable and conservative (real gain ≥ Modal).
